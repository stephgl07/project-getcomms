import React from 'react'

const Sha = ({params}) => {
  return (
    <>
    <div>{params.sha}</div>
    <div>{params.sha}</div>
    <div>{params.sha}</div>
    <div>{params.sha}</div>
    <div>{params.sha}</div>
    <div>{params.sha}</div>
    <div>{params.sha}</div>
    <div>{params.sha}</div>
    </>
  )
}

export default Sha